# CoverIQ Google Apps Scripts

| Script | Run once in editor | Spreadsheet created |
|--------|-------------------|---------------------|
| **Quote leads** | *(you already have this)* | *(existing)* |
| **User accounts** | `createSpreadsheet()` | CoverIQ User Accounts |
| **Agent accounts** | `createSpreadsheet()` | CoverIQ Agent Accounts |

You do **not** need to create Google Sheets manually. Each script creates its own file in Drive.

Folders: `coveriq-user-accounts/Code.gs` and `coveriq-agent-accounts/Code.gs`

We set them up **one at a time**. Reply **done** after each step.
